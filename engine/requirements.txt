fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.5.2
neo4j==5.24.0
anthropic==0.111.0
# NOTE: transformers + sentence-transformers + torch are intentionally NOT
# installed — they OOM a 512MB instance and the default (USE_LOCAL_ML=false) uses
# LLM sentiment + TF-IDF narratives instead. To run local ML on a >=2GB box:
#   pip install "sentence-transformers==3.1.1"
hdbscan==0.8.38.post1
scikit-learn==1.5.2
spacy==3.7.5
numpy==1.26.4
alembic==1.13.3
requests==2.32.3
# Full-article body extraction (keyless): turns headline-only article mentions
# into the whole readable article. Powers engine/ingestion/article_text.py.
trafilatura==2.1.0
# Scrapling (BSD-3): tier-2 page fetch with a real Chrome TLS/HTTP2
# fingerprint (curl_cffi) for hosts that 403 plain `requests`. The [fetchers]
# extra is what pulls curl_cffi; the playwright/patchright wheels it also
# brings are import-time deps of scrapling's engine module, not browsers —
# no `scrapling install` is run, so no Chromium is downloaded into the image.
scrapling[fetchers]==0.4.15
# yt-dlp (keyless): YouTube search + comments for engine/ingestion/youtube_connector.py.
# Google News RSS + Reddit connectors use only the stdlib + requests (no new dep).
yt-dlp>=2025.1.1
# Scweet (github.com/Altimis/Scweet): free X/Twitter scraping (no paid API),
# multi-account pooling + proxy. Powers engine/ingestion/scweet_connector.py.
# Needs a burner X login (X_USERNAME/X_EMAIL/X_PASSWORD) + open egress at
# runtime; enabled via ENABLE_SCWEET=true. Drives Chromium (present on deploy).
Scweet==3.0.0
# twscrape (github.com/vladkens/twscrape): the RELIABLE free X backend for this
# deploy — pure-HTTP (httpx), NO browser needed, so it runs on the slim image.
# Scweet above needs a Chromium binary not installed here, so twscrape is the X
# path that actually works headless. Powers engine/ingestion/twscrape_connector.py;
# enable with ENABLE_TWSCRAPE=true (+ the shared X_USERNAME/X_EMAIL/X_PASSWORD).
twscrape>=0.16
# Wikipedia is served in-process by engine/ingestion/wikipedia_connector.py
# (MediaWiki API, keyless). The standalone MCP equivalent, if wanted, is
# github.com/Rudra-ravi/wikipedia-mcp (`pip install wikipedia-mcp`).
# AgentReach (optional free web/social scraping — github.com/Panniantong/Agent-Reach).
# Install separately where ENABLE_AGENTREACH=true: `pip install agent-reach`.
# Left as a comment because its backends need open egress + optional CLIs.
